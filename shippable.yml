language: php

php:
  - 5.6
  
env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE
    - M2=TWO
    
build:
  pre_ci:
    - echo "This is pre_ci"
  ci:
    - echo "Hello, World!"
    - echo "This is master"
    - echo "This is ??????"
    - echo "Hello, World!"
    - echo "This is !!!!!!"

integrations:
  notifications:
    - integrationName: my_webhook
      type: webhook
      payload:
        - tag: $BRANCH_NAME:$BUILD_ID
      on_success: always
      on_failure: never
      on_pull_request: never
      on_start: never
