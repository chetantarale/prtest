build_image: chetantarale/u14php7pls:tip1

language: php

php:
  - 5.6
  
env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE
    - M2=TWO
    
build:
  pre_ci:
    - echo "This is pre_ci"
  ci:
    - apt-get install php5-dev
    - pecl install pdo
    - pecl install pdo_pgsql
    - sudo apt-get install pdo_pgsql
    - echo "Hello, World!"
    - echo "This is master"
    - echo "This is ??????"
    - echo "Hello, World!"
    - echo "This is !!!!!!"
