language: python

python:
  - 3.2
  
env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE
    - M2=TWO
    
build:
  pre_ci:
    - echo "This is pre_ci"
  ci:
    - echo "Hello, World!"
    - echo "This is master"
    - echo "This is ??????"
    - echo "Hello, World!"
    - echo "This is !!!!!!"
