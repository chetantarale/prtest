language: php

php:
  - 5.6
  
env:
  global:
    - FOO=FUBU
    
build:
  pre_ci:
    - echo "This is pre_ci"
  ci:
    - echo "Hello, World!"
    - echo "This is master"
    - echo "This is ??????"
    - echo "Hello, World!"
    - echo "This is !!!!!!"

integrations:
  notifications:
    - integrationName: my_webhook
      type: webhook
      payload:
        - tag=$BUILD_NUMBER my_hook
      on_success: always
      on_failure: never
      on_pull_request: never
      on_start: never
    - integrationName: hook2
      type: webhook
      payload:
        - tag=$BUILD_NUMBER hook2
      on_success: always
      on_failure: never
      on_pull_request: never
      on_start: never
