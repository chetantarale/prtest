language: python

python:
  - 3.2
  
env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE
    - M2=TWO
    
build:
  pre_ci:
    - echo "This is pre_ci"
  ci:
    - echo $JOB_ID
    - echo $SHIPPABLE_JOB_ID
    - echo $SHIPPABLE_BUILD_ID
    - echo "Hello, World!"
    - echo "This is master"
    - echo "This is ??????"
    - echo "Hello, World!"
    - echo "This is !!!!!!"
