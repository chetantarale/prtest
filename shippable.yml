build_image: chetantarale/u14php7pls

language: php

php:
  - 5.6
  
env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE
    - M2=TWO
    
build:
  pre_ci:
    - echo "This is pre_ci"
  ci:
    - echo $JOB_ID
    - echo $SHIPPABLE_JOB_ID
    - echo $SHIPPABLE_BUILD_ID
    - sudo apt install pdo_pgsql
    - echo "Hello, World!"
    - echo "This is master"
    - echo "This is ??????"
    - echo "Hello, World!"
    - echo "This is !!!!!!"
